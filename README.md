1. Install the scrapy lib
2. Install the pandas lib

The both dependencies can be installed from pip3

To create a csv file with the salaries data, execute the following command:

`scrapy runspider robot.py -o salary.csv`

Code what you want in `sj_salaries.py` file and run it.
